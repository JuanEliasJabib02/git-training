console.log("second commit ") // start git on the repository


// git add . --> Add all files to the staging area
 
// git commit -m "text" -->   send the version to the local repository


// git am -m "text" --> is the git add and commit in the same time

// git push --> send the version to the cloud repository (github)

// git clone --> For clone the respository

// git branch --> Make new branchs

// git checkout --> swap between branchs

// git checkout -D --> delete a specific branch of the repository

//git merge --> to fusion a sub-branch with the the main branch

// git push --set-upstream origin [branch] --> for send the new branch to the remote repository